server:
  port: 8094
  
spring:
  application:
    name: API_GATEWAY


  cloud:
    gateway:
      routes:
      - id: user-service
        uri:
          lb://user-service 
        predicates:
        - Path=/helloWorld1
        
      - id: order-service
        uri:
          lb://order-service 
        predicates:
        - Path=/helloWorld2
        
      - id: supplier-service
        uri:
          lb://supplier-service 
        predicates:
        - Path=/helloWorld3   
        
          

